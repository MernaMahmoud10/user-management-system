export interface LoginInps {
    username: string;
    password: string
}
export interface User {
    username: string;
    email: string;
    phone: string;
    birthDate: string;
    image: string;
}